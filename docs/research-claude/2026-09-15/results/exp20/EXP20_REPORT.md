# EXP-20 보고서 — via R 표피효과 (m), 벽 표피 R 결합 (mk)

실행 2026-09-16, 기준선 exp13/j, 사전 등록 `EXP20_PLAN.md`(§1 규칙·§3 기준 불변). 러너 `exp11/run11.py --variant m|mk`, 비교 `table11.py --exp exp20 --baseline exp13:j`. 자유 계수 없음(SOLID via는 원형 도체 Bessel 정확해, HOLLOW는 두께 t_p의 Zs 폐형식; DC 극한은 기존 값과 일치). 구현 자체 검증: 합성 SOLID(D 60 µm) R(1 kHz)/Rdc − 1 = 9e-10, R(100 MHz)/Rdc = 2.57; HOLLOW(D 200 µm) 8e-10, 3.08. 기본 경로 불변: `--variant none --smoke` 5.83e-12, `--variant j` 0.0, `smoke_port18.py` PASS. P18 레일 via 30,520개 중 SOLID 30,169·HOLLOW 351; via R 비율 중앙값 1 MHz 1.0002, 10 MHz 1.018, 100 MHz 1.80.

## 1. 7케이스

| 변형 | ≤ 1 MHz max \|ΔZ\|/\|Z\| | PASS 변화 | G3 중앙값 | 10–100 MHz ΔR/Re Z_ref 중앙값 (기준선 −0.144) | (a) | (b) |
|---|---|---|---|---|---|---|
| m (via 표피 R) | 9.5e-5 | 없음 | 6.32 → 6.37 % | −0.141 | 성립 | **불성립** |
| mk (m + 벽 표피 R) | 6.6e-4 | 없음 | 6.32 → 6.37 % | **−0.031** | 성립 | 성립 |

케이스별 r_HF(m): P14 −0.175 → −0.160, P16 −0.293 → −0.274, P18 −0.061 → −0.050, P19 −0.246 → −0.222, P1 −0.144 → −0.141, P7 −0.029 → −0.012, 804-P18 −0.109 → −0.102. via 표피 R 단독은 7케이스(대형 평면 레일 위주, via R 비중 작음)에서 1–2 %p만 움직인다. mk는 EXP-18b k2(−0.038)와 거의 같은 −0.031로, 7케이스의 개선은 벽 항이 주도한다. 계획 §3에 따라 **mk만 92포트 실행**하고 m은 기록만 한다. G4는 m에서 P7·P14·P16·P18·804-P18 소폭 개선(예: P18 0.177 → 0.166).

## 2. 92포트 (mk) — **채택 문턱 미달, 기록만**

`runall15.py --variant mk --outdir exp20`, 92포트 성공(실패 0). 비교 `compare92_mk_vs_j.json`(기준선 EXP-15/j), H3 지표 `dl17.py --dir exp20 --variant mk`.

| 지표 (92포트) | 기준선 j | k2 (EXP-18b) | mk | 채택 문턱 |
|---|---|---|---|---|
| ≤ 1 MHz max \|ΔZ\|/\|Z\| | — | 9.1e-4 | 9.7e-4 | ≤ 1e-3 ✓ |
| PASS 집합 변화 (G1–G4: 8/10/30/0) | — | 없음 | 없음 | 없음 ✓ |
| 10–100 MHz ΔR < 0 비율 | 0.98 | 0.92 | 0.92 | ≤ 0.70 ✗ |
| 중앙값 \|ΔR\|/Re Z_ref | 0.254 | 0.234 | **0.203** | ≤ 0.15 ✗ |
| G5 최대 오차 중앙값 | 0.366 | 0.362 | 0.356 | (보고만) |
| G4 오차 중앙값 | 0.325 | 0.318 | 0.318 | (보고만) |

via R 비율(100 MHz) 포트 중앙값 2.57(SOLID 60 µm via 지배). 개선이 큰 포트: Port1 −0.144 → +0.041, Port51/Port5(REF_CLK) −0.35 → −0.17, Port25/Port71 −0.47 → −0.32. 두 표피 항(벽 + via)을 합쳐도 92포트 고주파 R 부족의 중앙값은 25 % → 20 %로만 줄었다. 남은 부족은 EXP-19 H_e가 가리키는 대로 **주파수와 무관하게 참조가 더 큰 급전 경로 R 관례**(저주파 결손과 같은 기원)이며, 표피효과 폐형식으로는 메워지지 않는다.

## 3. 판정
- m: (b) 불성립 → 기록만. mk: 7케이스 (a)(b) 성립 → 92포트 실행 → 채택 문턱 미달 → **기록만, 기준선 exp13/j 유지**.
- 두 표피 항(k2, m)은 물리적으로 타당하고 자유 계수가 없으며 게이트를 해치지 않는다. 최종 정식화에는 포함하는 것이 맞지만, 채택은 급전 경로 R 관례가 해결된 뒤 한꺼번에 판단한다(개별로는 문턱 미달).
- G1–G5 정의 불변.

## 4. 산출물
`result_*_any_{m,mk}.json`(m 7, mk 93), `compare_m.json`, `compare_mk.json`, `compare92_mk_vs_j.json`, `exp17/exp17_*_exp20.*`. 저장소 사본은 계획·보고서·비교 JSON.
