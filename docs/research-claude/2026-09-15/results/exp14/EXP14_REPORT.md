# EXP-14 보고서 — [구현 차이] 되돌리기 (e) fringing, (f) 균질화 L, (g) via 배럴 면적, (h) via L two-wire

실행 2026-09-16, 7케이스, 기준선 = EXP-13 변형 j(EXP-8 + c_unit_fix, 소유자 승인). 사전 등록 `EXP14_PLAN.md`(§4 기준 불변). 러너 `exp11/run11.py --variant e1|e2|f|g|h`, 비교 `table11.py --exp exp14 --baseline exp13:j`. 기본 경로 불변 검증: `--variant none --smoke` 5.83e-12, `--variant j --smoke --smoke-baseline exp13:j` 0.0, `smoke_port18.py` PASS.

## 1. 요약

| 변형 | max \|ΔZ\|/\|Z\| (≤10 MHz, 7케이스 최대) | G3 중앙값 | PASS 변화 | 판정 |
|---|---|---|---|---|
| (e1) fringing 문턱 제거 | 1.9e-2 | 6.32 → 6.32 % | 없음 | 기본값 유지(adopt=False) |
| (e2) fringing 상한 제거 | 1.6e-1 | 6.32 → 8.02 % | G2 FAIL ×3(P16, P18, 804-P18), G3 FAIL ×1(804-P18) | 채택 불가(사전 선언) |
| (f) 외부 L에 1/G 미적용 | 3.9e-2 | 6.32 → 6.33 % | 없음 | 기본값 유지(adopt=False) |
| (g) via 배럴 정확 면적 | 6.0e-3 | 6.32 → 6.34 % | 없음 | 기본값 유지(adopt=False) |
| (h) via L two-wire(2배) | 8.1e-1 | 6.32 → 7.21 % | G2 FAIL ×3(P16, P18, 804-P18) | 채택 불가 |

어느 변형도 §4 채택 기준(FAIL→PASS 하나 이상 또는 G3 중앙값 −1 %p)을 만족하지 않았다. H_null은 모두 불성립(변화는 있으나 게이트에 못 미침).

## 2. 변형별 결과

**(e1)** 모든 엣지에 상한 있는 fringing 적용. 엣지 fringing 비율 1.0, 평균 weff/w는 L14 0.988·L25 0.997(상한이 G≈1 셀을 보호). ΔL −0.05~−1.0 pH(P19만 +2.6), err 1 MHz P1 3.40→3.32 %, P18 2.69→2.67 %. 예측(|ΔL| ≤ 2 pH, 게이트 불변)대로다.

**(e2)** 상한 제거. 유효폭 평균 L14 1.22배, L20/L21 3.3–3.7배(fine 블록). ΔL은 P1 −9 pH(39→30, 개선)이지만 P18 +7.6, 804-P18 +24 pH로 악화(fine 블록 안 셀 간 L 배분이 뒤틀려 총 L이 오히려 증가). 비물리 상한 실험으로 사전 선언대로 채택하지 않는다. 예측(전 케이스 ΔL 음)은 P1 외에는 빗나갔다.

**(f)** 외부 L에 균질화 1/G 미적용. ΔL −0.2~−3.1 pH(P19 +4.2), err 1 MHz P1 3.40→3.14 %, P18 2.69→2.63 %, 804-P18 4.89→4.82 %. 예측(−2~−20 pH)보다 작다. 1/G 보정분이 평면 L의 1–2 %뿐이라 EXP-3 S1 결론(균질화는 L을 거의 안 바꿈)과 일치한다.

**(g)** 도금 배럴 via만 R·D/(D−t_p). 스케일된 via는 351/30,520(P18), 19/705(P19) 등 core PTH뿐이다. ΔRe ≤ +0.005 mΩ(P1 +0.005, P18 +0.0013), P19 err 29.90→29.60 %. 예측(≤ +0.02 mΩ, 게이트 불변)대로다. **port19 R 결손(≈5.3 mΩ)의 원인이 아님**을 확인했다(배럴 면적 정정으로 얻는 R은 0.07 mΩ).

**(h)** via L 2배. ΔL은 계획 §2 표의 via L만큼(P18 +5.3, P1 +43, P14 +36, P19 +300 pH) 늘어 G2가 3케이스 FAIL. 보고만: **G5(10–100 MHz)는 크게 개선**됐다 — P14 0.405→0.220, P16 0.369→0.258, P18 0.279→0.165, 804-P18 0.216→0.142. 즉 저주파에서는 현재 via L(동축 절반값)이 맞고 고주파에서는 2배가 더 맞는다. 이는 주파수에 따라 via 유효 L이 커지는(귀환 전류가 평면에서 via 주변으로 몰리는) 효과를 시사하며, §6-6(10–100 MHz) 과제의 단서로 남긴다.

## 3. 판정
(e1)(f)(g)(h)(e2) 모두 기본값 유지. G1–G5 정의 불변. 영수증 `result_*_any_{e1,e2,f,g,h}.json`, `compare_*.json`.

## 4. §6-4 되돌리기 과제 종결 상태
(a)(b)(c)(d)(e)(f)(g)(h) 완료, (i) 실장 루프 L은 decap 실장 기하가 필요해 미착수. 채택된 것은 산술 오류 수정(j)뿐이며 기준선으로 승인됐다. 게이트를 바꾼 변형은 없다. 개선 방향의 작은 신호는 (e1)(f)(g)에서 각각 ΔL −1~−3 pH·ΔRe +0.005 mΩ 수준이고, 셋을 합쳐도 G3 중앙값을 1 %p 움직이지 못한다(개별 합산 추정 6.32→약 6.2 %). 결합 실험은 하지 않았다.
