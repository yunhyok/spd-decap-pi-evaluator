# EXP-15 계획 — 260729 92포트 일반화 검증 (기준선 exp13/j)

작성 2026-09-16, 실행 전 사전 등록. 인계 §6-5. 결과를 본 뒤 게이트·집계 규칙·홀드아웃을 바꾸지 않는다.

## 1. 대상과 동결 규칙
- 설계 260729, 참조 npz의 92포트 전부(SITE0 46 + SITE1 46, 레일 92개 서로 다름). 포트명은 npz `port_names`의 `::` 앞부분 그대로(`Port5_SITE0_1721` 같은 접미사 포함).
- 모델: EXP-8 cavity-wall 동결형 + `c_unit_fix=True`(= exp13/j 기준선). 파라미터·주파수 사다리·게이트 계산은 `run11.py --variant j`와 동일. 포트별 영수증 `WORK_DIR/exp15/result_260729_{port}_any_j.json`.
- 홀드아웃: s5m6585(160포트)는 이번에 열지 않는다. 260804는 P18만 이미 열려 있으며 추가로 열지 않는다.
- 이미 실행된 6포트(P1, P7, P14, P16, P18, P19)는 exp13/j 영수증과 동일해야 한다(재현 검사, 상대차 ≤ 1e-9). 다르면 결과 전체를 무효로 하고 원인만 보고한다.
- 실패 처리: 추출·빌드·풀이가 예외로 끝난 포트는 `failures.json`에 포트명·예외를 기록하고 집계에서 "실패"로 센다. 실패 포트에 맞춰 코드를 고치지 않는다(원인만 보고).

## 2. 사전 예측(가설)
- 7케이스에서 G3 PASS 4/7, G1 PASS 4/7, G4 PASS 0/7이었다. 예측: 92포트 G3 PASS 비율 ≥ 50 %, G1 ≥ 50 %, G4 ≤ 20 %.
- G3 FAIL의 과반은 R 결손형(100 kHz에서 dRe = Re Z_model − Re Z_ref < −0.05 mΩ, 즉 참조 R이 더 큼)이다(P14·P16·P19형). L 과다형(fit_dL > +5 pH, P1형)은 소수다.
- 같은 레일의 SITE0·SITE1 포트는 같은 게이트 결과를 낼 것으로 본다(불일치 ≤ 10 %).

## 3. 집계(동결)
포트별: G1–G5 지표, PASS 플래그, err_1MHz, dRe_100k, fit_dL, f_res(모델/참조), unknowns, wall, 메모리. 전체: PASS 비율(G1–G4), err_1MHz 분포(중앙값·사분위·최대), 실패 수. 분류: G3 FAIL을 "R 결손형(dRe_100k < −0.05 mΩ)", "L 과다형(fit_dL > +5 pH)", "기타"로 나눈다(경계값은 이 문서에서 고정). SITE0/SITE1 게이트 일치율.

## 4. 판정
가설 검증 실험이며 채택/기각 대상 파라미터는 없다. §2의 세 예측 각각을 확인/기각으로 보고한다. G1–G5 정의 불변.

## 5. 산출물
`result_260729_*_any_j.json` ×92(실패 제외), `failures.json`, `summary15.json`, `EXP15_REPORT.md`, 히스토그램 `exp15_err1M_hist.png`. 저장소 사본 `results/exp15/`(영수증은 용량 때문에 `summary15.json`·보고서·그림만; 원본 영수증은 WORK_DIR).
예상 비용: 포트당 40 s–15 min, 14 병렬(20 CPU, 64 GB, 포트당 ≤ 2.1 GB) → 약 1–2 시간.
