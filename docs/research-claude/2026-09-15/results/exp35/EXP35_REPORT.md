# EXP-35 보고서 — 표피 손실 항(via + 벽)을 기준선 p 위에서 재시험 (변형 pmk)

실행 2026-09-17, 사전 등록 `EXP35_PLAN.md`(§3 기준 불변). 기준선 exp28/p. 러너 `run11.py --variant pmk`(첫 실행은 변형 정의의 키 누락으로 영수증 저장 실패 → 정의 수정 후 재실행), 비교 `table11.py --exp exp35 --baseline exp28:p --variant pmk`, H3 지표 `dl17.py --dir exp35 --variant pmk`, `compare92_pmk_vs_p.json`.

## 1. 7케이스
게이트 PASS 집합 불변, ≤ 1 MHz max |ΔZ|/|Z| ≤ 2e-2(1 MHz 이하 변화 없음), G3 중앙값 6.56 → 6.51 %. 10–100 MHz ΔR/Re Z_ref 중앙값 **−0.161 → −0.056**(P18 −0.086 → +0.005, P1 −0.161 → +0.021, P7 −0.072 → +0.017, P19 −0.352 → −0.303). (a)(b) 성립 → 92포트.

## 2. 92포트

| 지표 | p | pmk | 채택 문턱 |
|---|---|---|---|
| ≤ 1 MHz max \|ΔZ\|/\|Z\| | — | 9.7e-4 | ≤ 1e-3 ✓ |
| PASS 집합 변화 | — | 없음 | ✓ |
| 10–100 MHz ΔR < 0 비율 | 1.00 | 0.95 | ≤ 0.70 ✗ |
| 중앙값 \|ΔR\|/Re Z_ref | 0.379 | 0.339 | ≤ 0.15 ✗ |
| G5 최대 오차 중앙값 | 0.283 | 0.282 | (보고만) |

## 3. 판정
- **기록만**(EXP-20과 같은 결론). 두 표피 항은 대형 평면 레일(7케이스 다수)에서는 고주파 R 부족을 거의 없애지만, 92포트 대다수(급전 경로 지배)에서는 부족의 약 1/10만 메운다. 남은 고주파 부족은 저주파 결손과 같은 기원(급전 경로 R 관례, EXP-19/31)이다.
- 두 항은 물리적으로 타당하고 게이트를 해치지 않으므로, 급전 경로 관례가 해결되는 시점에 최종 정식화에 함께 넣는 것을 권한다(개별 채택은 문턱 미달).
- 기준선 exp28/p 유지. G1–G5 정의 불변.
