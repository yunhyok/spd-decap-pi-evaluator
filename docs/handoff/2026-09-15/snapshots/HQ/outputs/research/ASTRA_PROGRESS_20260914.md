# 최종 상태: 잔여 사용량 40% 조건으로 전체 중지

연구·계산·Sol/Astra 검토 및 하위 에이전트 작업을 모두 종료했다. 사용자 명시 재개 전 새 작업·재시도·Reset은 하지 않는다. 최종 기록: HQ_STOPPED_CHECKPOINT_20260914_USAGE40.md.

마지막 정적 해법 비교는 노드 오차를 18.1286mA에서 4.30261mA로 줄였지만 기준 0.0001mA를 충족하지 못했다. 두 표본 모두 미수렴이며 부호 검사 실패도 보존했다. 표본당 42~44초가 소요되어 현재 방식의 비용도 확인했다. 실제 보드의 채택된 1MHz 오차는 26.18%로 그대로다.

Astra 평가: 물리 항과 수치 오차를 분리하는 진단은 진전이지만 최초 정확도 목표는 미달성이다. 앞선 실행에서 좌측/우측 보정 방식 차이를 사전 검토가 놓쳤고, 제한 시간 내 최종 검증 여유도 부족했다. 재개 시 저장된 증거를 바탕으로 정적 보조 해법의 노드 보존 문제를 우선 검토하도록 기록했으며 추가 계산은 시작하지 않았다.

아래 내용은 이전 시점의 진행 기록이다.

# SPD Decap PI Evaluator v0.23.1 — Astra 연구 상황보고

**정적 보조 해법 진단 완료:** 자기장 연산 없이 38.6초 동안 기존 보조 해법을 두 번 적용했다. 노드 성분이 없는 실제 오차 벡터를 입력했는데도 최대4.934A의 노드 오차가 생겼고, 현재 노드 오차만 보정한 경우에도 **18.1286mA → 18.1634mA**로 줄지 않았다. 두 표본에서는 현재 보조 해법의 정확도가 부족하다는 근거를 얻었다. 더 정확한 정적 해법이 실제로 개선하는지는 아직 측정하지 않았다.

별도 부호 일치 검사는6.05pA의 차이로 기존 기준을 넘었다. 실패 상태와 원래 허용오차를 유지했고, 측정값은 실패 전에 모두 저장했다. 독립 검토는 이 작은 차이가 위의 큰 오차나 보정 효과 부재를 설명할 수준은 아니라고 판단했다. 전체 검사 통과나 보드 정확도 개선으로 표시하지 않는다.

최신 공식 사용량은42%이며40% 이하 중지 조건은 아직 관측되지 않았다. 같은 두 표본에 더 정확한 정적 역연산을 적용하는 제한된 비교 코드를 준비 중이다. 새 계산 직전에 다시 확인하며,40% 이하가 관측되면 당시 진행 중인 준비·저장만 마치고 새 계산을 시작하지 않는다.

**저장 전류장 후속 확인 완료:** 우측 보정 후 최대 노드 전류 보존 오차는 **49.98mA → 18.13mA**, 약2.76배 줄었다. 하지만 허용 기준0.0001mA보다 여전히 크므로 이 전류장을 채택할 수 없다. 전체 자기장 검증이 미완료인 사실과, 노드 전류 보존 기준 실패가 직접 확인된 사실을 구분한다. 원시 포트 값이 변했어도 정확도 개선으로 해석하지 않는다. 확인된 보드 1MHz 오차26.18%는 그대로다.

다음 작업은 자기장 연산을 추가하는 대신, 현재 정적 보조 해법에 두 개의 실제 오차 벡터를 적용해 노드 보존 오차 발생량을 측정하는 진단이다. 기존 보조 행렬 구성을 재사용하고 자기장 함수가 호출되면 즉시 실패하도록 제한한다. 한 번의 행렬 인수분해·두 번의 보조 해법 적용,180초·32GiB 범위로 준비 중이다. 아직 이 정적 진단은 실행하지 않았다.

**14시 42분 후속: 우측 보정 진단은 최종 검증 도중 시간 제한으로 중단됐다.** 900.843초에 정해진 900초 제한이 적용됐으며, 메모리 초과는 아니었다. 자기장 연산 7회는 완료됐고 보정된 전압·전류장을 저장했지만, 마지막 8번째 자기장 검증은 끝나지 않았다. 따라서 이 실행을 수렴 성공이나 수렴 실패로 단정하지 않는다. 전체 잔차·전력·자기장 구성식·solver 종료값은 확인되지 않았으며 정확도 개선 결과도 없다.

계산식의 우측 보정 변경과 입력 재사용 경로는 검토·실행 기록에서 확인됐다. 다만 실제 연산 시간이 회당 약100~115초로 늘어 최종 검증을 마칠 시간 여유가 부족했다. 원래 제한과 실패 기록은 유지했다. 저장된 보정 전류장으로 자기장 계산이 필요 없는 노드 전류 보존 오차만 확인 중이며, 이 값으로 다음 정적 보조 해법 진단의 필요성을 판단한다. 추가 자기장 연산이나 반복 cycle은 시작하지 않았다.

**14시 후속 결과: 실제 보드의 유한 전류 갱신 계산은 수렴하지 않아 채택하지 않았다.** 22회 자기장 연산을 36.9분에 수행했고 최대 private memory 27.55GiB로 정해진 한도 안에서 종료했다. 원래 방정식의 상대 잔차는 1.7997, 최대 전류 보존 오차는 49.98mA, 전력 불일치는 임피던스 환산 424.08µΩ이다. 모든 전류·자기장과 실패 기록을 보존했다. 현재 확인된 실제 보드 1MHz 복소 오차는 **26.18%로 그대로**다.

기준 해석의 우측 전처리 방식과 이번 코드의 좌측 전처리 방식이 달랐다는 점을 사후에 발견했다. 네 개의 정적 보조 행렬은 재사용했지만 해법 전체가 같다는 사전 검토 판단은 잘못이었다. 이는 HQ와 Sol 검토에서 놓친 부분이며, 물리 연결 검사를 통과한 것만으로 실행 방식까지 일치한다고 판단한 점을 바로잡는다. 이 차이가 수렴 실패의 유일 원인이라는 증거는 아직 없다.

추가 대형 계산 없이 저장된 결과만 4.8초 동안 분석했다. 자기항을 켠 초기 잔차 222.718에서 최종 1.7997로 약124배 줄었지만, 남은 오차는 주로 노드의 전류 보존 식에 몰려 있다. 저장된 두 전류장을 가장 유리하게 섞어도 잔차 개선은 0.00345%뿐이므로 단순 재조합은 해결책이 아니다. Sol은 원 배열을 독립 검증하고, Astra는 같은 물리 조건에서 우측 보정 6단계·새 자기장 연산 최대8회·900초·32GiB로 제한한 진단 1회를 준비하도록 했다. 현재 추가 계산은 아직 시작하지 않았다. 이 진단 뒤 같은 계산을 자동 반복하지 않는다.

**진행 평가:** 실제 보드에 연결해 실패를 정량화한 것은 진전이다. 그러나 아직 정확도 개선 결과는 없고, 사전 검토가 놓친 해법 차이로 36.9분의 계산 비용이 발생했다. 검사 통과 수를 성과로 세지 않고, 다음 판단은 실제 잔차와 포트 오차를 줄였는지에 둔다.

아래는 이전 시점의 준비·완료 이력이다. 현재 상태는 위 내용을 우선한다.

**후속 실험 준비:** 기존 저항 모델을 보존하는 L14 전류 연결 검사와, 전류 갱신용 L14·L25 자기장 계산 검사를 통과했다. 전류 갱신 코드의 실제 규모 1회 계산은 약100초, 전체 검증105.6초·최대5.73GiB였고, 기존 저장 결과와의 차이는2e-15 미만이다. L14 자기항의 미계산1,807개 형상도 기존 결과를 보존하며 모두 채웠다. 추가 비용은 약71초와 마지막 두 형상 약12초였다.

현재 Sol/Terra가 이 코드를 실제 보드 해석에 연결하고 있다. 초기 제출 코드에는 실제 보드 계산 연결이 아직 빠져 있어 HQ가 완료로 인정하지 않고 보완을 지시했다. 자기항을 끈 상태의 실제 방정식 재현, 전류가 바뀐 상태의 전력·잔차 검증, 실측100초/회에 맞는 실행 제한을 갖춘 뒤 한 번의 유한 전류 갱신 실험을 실행한다. 아직 새 보드 임피던스는 계산하지 않았다.

**최신 완료: L04 반환 전류를 포함한 계산 PASS.** 같은 물리 입력을 성분별로 나눠 계산하여 357.4초, 최대 private memory13.46GiB로 완료했다. 기존 900초·32GiB 제한을 유지했고, 실패했던 첫 실행 기록도 보존했다. 직접 합산 최대 상대 차이2.17e-8, 양방향 결합 일치 차이4.87e-10이다.

1MHz 고정 전류 기준의 전원층–L04 양방향 결합은 -1.709-j43.024mΩ으로 크게 상쇄한다. 하지만 L04 자체 항은 1.967+j40.502mΩ이며, 모든 해당 항을 합치면 기존 전원층만의0.158+j27.467mΩ에서 **0.416+j24.945mΩ**로 변한다. L04 반환 전류를 포함하는 것만으로 큰 부분 자기항이 사라질 것이라는 예상은 성립하지 않았다. 이 값은 실제 보드 Z 변화가 아니며, 보드의 확인된 1MHz 오차26.18%는 그대로다.

현재 Sol은 저장된 전체 결과를 독립적으로 재합산하고, Astra는 남은 전류 경로·근접 적분·전류 재분배를 구분할 다음 최소 보드 연결 실험을 검토한다. 새 전체 보드 계산은 아직 실행하지 않았다.

아래는 준비와 이전 결과의 이력이다.

**12시 후속: 저장된 L04 반환 전류의 재사용 조건을 확인했고, 메모리 문제를 수정 중이다.** 현재 기준 전류와 L04 경계 입력이 정확히 일치하여 새 전류 복원은 필요 없다. 첫 결합 실행은 8.484초에 프로세스 private memory 34.03GiB로 32GiB 한도를 넘어 중단됐다. 물리 메모리 고갈이나 물리 모델 실패로 판단하지 않는다. 실패 기록을 보존했다.

전원층끼리의 완료 결과를 재사용하고 새 결합을 8개 실수 성분으로 순차 계산하도록 최소 변경했다. 설치된 라이브러리의 결합 호출이 자체 항을 중복 계산하는 경로는 동일한 검증된 native 연산을 직접 호출해 피한다. 19개 source/11개 target의 복소 전류 직접 대조에서 최대 상대 차이2.04e-16, 입력 및 runtime 해시 검증을 통과했다. 이 값은 작은 호출 검증이며 전체 규모 메모리나 실제 보드 정확도 개선 증거가 아니다. Sol이 변경을 검토 중이며 900초·32GiB 상한을 유지한다.

아래는 이전 완료 결과다. 실제 보드의 확인된 1MHz 오차는 **26.18%로 그대로**다.

**최신: 실제 저장 전류의 L14·L25 자기 결합 계산 완료.** 약79.4만 점을 154초, 최대10.2GiB로 계산했다. 직접 합산 및 양방향 상호성 검사를 통과했다. 다만 두 전원 시트만의 고정 전류 합계는 약0.158+j27.467 mΩ으로 크며, 기존 보드 오차를 줄였다는 뜻이 아니다. 리턴 경로를 포함하지 않은 값을 보드 보정으로 사용하지 않는다.

L14 자기항의 미계산분 상한은0.4nΩ 미만으로 줄였다. 반면 같은 층의 선택 근접6쌍에서 약21.5μΩ, 층간 일부 강한 쌍에서 쌍당0.86~3.72μΩ의 중심점 근사 차이가 확인됐다. 근사 오차와 빠진 리턴 전류를 분리해 다룰 근거가 생겼다.

현재 Sol/Terra는 기존 저장 전류·노드 좌표에서 영향이 큰 누락 리턴 구간을 찾고, Astra는 이를 실제 보드 해석에 반영할 최소 변경 경로를 검토한다. 전체 보드 재구축이나 같은 대형 계산 반복은 시작하지 않았다. **실제 보드의 확인된1MHz 오차는26.18%로 그대로이며, 정확도 개선은 아직 미확인이다.**

아래는 앞서 완료한 작은 연결 문제의 기록이다.

실제 원본 형상으로 만든 작은 연결 문제에서 **교차항 보정의 포트 영향**을 처음 확인했고, 수치 오차와 한 단계 공간 세분화의 영향을 따로 비교했다. 최초 목표인 실제 보드의 PowerSI 수준 정확도는 아직 달성하지 않았다.

| 1MHz 계산 | 복소 임피던스 Ω | 확인 결과 |
|---|---:|---|
| 원래 두 리턴 셀, point+self | 0.9871705921 + j0.1494403986 | 직접/반복해·수지·수동성 통과 |
| 같은 조건, 검증된05 교차항만 추가 | 0.9871704741 + j0.0978202287 | 보정에 따른 상대 변화5.203619% |
| 같은 형상/전극, 리턴 셀2→3 세분화 | 0.9858415393 + j0.0978647516 | 공간 세분화 상대 변화0.134218% |

보정 비교에서는 R/D/H/P/종단과 다른 모든 자기장 계수를 고정했다. 공간 비교는 같은 전극 면을 둘로 나누되 각 면의 전류를 독립으로 풀었으며, 원래05 계수를 새로운 기저에 이식하지 않았다.

원래 공간 기저를 유지하고 세분화된 적분만 되돌려 적용한 비교에서 Z 차이는2.2065×10⁻⁹ Ω이었다. 실제 공간 기저를 늘린 변화는0.00132968 Ω이었다. 보정 자체의 변화는약0.05162017 Ω으로, 이번에 관측한 적분/단일 공간 세분화 변화보다 크다. 한 번의 세분화만으로 전체 메시 수렴을 인증하지 않는다.

직접해와 반복해의 차이는 최종 원래 메시에서최대5.60×10⁻¹⁶ Ω, 세분화 메시에서1.15×10⁻¹⁶ Ω이었다. 전류·전하·복소 전력 수지와 손실의 비음수 조건을 통과했다. 손실 행렬에 양의 정부호를 잘못 요구했던 최초 FAIL은 양의 준정부호와 계산 반올림 오차에 맞게 바로잡았으며, 원 행렬·고유값을 조작하지 않았다. 실제로 실패한 저차 구적 결과는 보존했다.

이번 도체와 전극은 원 좌표를 유지하지만 원 보드와 다른 연구용 경계다. 따라서5.203619%는 **이 작은 문제의 보정 전후 변화**이며, 실제 보드의 정확도 개선율이 아니다. 현재 확인된 실제 보드1MHz 오차26.1826839757%는 그대로다.

## 지금까지의 진행에 대한 평가

기존의 형상·전류 소유·자기항 검증은 필요한 작업이었고, 이번 결과로 일부 보정이 실제 포트 계산에도 영향을 준다는 근거를 얻었다. 다만 그동안 보조 검증과 전체 모델 복원에 비해 최종 포트 결과를 얻는 단계가 늦었다. 진행 관리에서는 이 부분을 보완해야 한다.

이번에는 Astra가 물리 조건과 판별 순서를 정하고, Sol/Terra가 형상·R/D/H를 구현하며, 별도 Astra task가 독립 검토를 맡았다. HQ는 작은 L/P와 두 종류의 포트 비교를 통합했다. 기존 산출물을 재사용했고 전체 보드 조립이나12,664쌍 적분을 선행하지 않았다.

다음 작업은 기존 실제 보드1MHz 계산에서 이 보정을 올바른 전류 기저에 연결할 수 있는 지점을 찾는 것이다. Sol task가 원래 행렬·포트·실행 조건과 연결 경로를 읽기 전용으로 확인 중이다. 연결이 없으면 빠진 부분을 정확히 특정한 뒤 최소 추가 작업을 정한다. 다른 전류 기저에 계수만 옮겨 보드 개선을 주장하지 않는다.

추가 연결 조사에서는 기존 보드에 해당 자기장 보정의 직접 삽입 경로가 없고, 선택한 네 경로의 전류도 약1~6 nA라는 점을 확인했다. 작은 문제에서는1 A가 흐르도록 전극을 선언했으므로 그5.2% 효과를 원 보드에 적용할 수 없다. 따라서 큰 보드 해석을 새로 만들기 전에 저장된 전류로1차 포트 감도와 상쇄 없는 지표를 계산하고, 이미 검증된 일곱 쌍 중 실제 영향이 있는 후보를 고르는 중이다. 기존 보드 오차는 크기 약0.038 dB, 위상 약15.1°로 나타난다. 이 지표와 현재 모델에 포함되지 않은 자기장 항을 함께 고려한다.

이후 일곱 쌍의 저장 전류 감도 비교를 완료했다. 가장 큰1차 영향은7.48×10⁻¹⁷ Ω, 상쇄 없는 스펙트럴 상한은2.93×10⁻¹⁴ Ω으로 기존 보드 오차의약1.38×10⁻¹⁰배 이하다. 해당 위치들을 위한 추가 연결 구현과 대형 재계산은 진행하지 않는다. 다음 우선순위는 기존 L14/L25 자료를 재사용해 자기항과 층간·리턴 상호작용의 기여를 분리하는 것이다. 이 감도 검사는 현재1MHz 전류에서의 미분값이며, 유한 변화나 전체 리턴·다른 주파수의 중요성을 기각하지 않는다.

## 근거 파일

- 원래 메시의 동일 조건 비교: `astra-source-fixture-port-20260914-q8-n64/result.json`
- 공간 세분화 비교: `astra-refined-fixture-port-20260914-01/result.json`
- 적분/공간 분리: `astra-refined-fixture-port-20260914-01/coarse-projected-integrals/summary.json`
- 구적 변화: `astra-source-fixture-summary-20260914.json`
- 실제 입력과 다음 작업: `HQ_ACTIVE_CHECKPOINT_20260914.md`

위 경로는 이 문서가 있는 `outputs/research` 기준이다. 별도 구현 task는06f1, 물리 검토 task는353f 작업 공간에서 운영했다. 프로그램 배포·제품 버전 변경·commit/push는 수행하지 않았다.
